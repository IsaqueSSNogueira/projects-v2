<!-- ::before ::after -->
## ::before ::after
- Montam um pseudo-elemento;
- Não esquecer do `content: "";`
- Usar `::before` e `::after` (padrão moderno)
- Pode misturar pseudo-classes:
  `elemento:checked::before {}`