<!-- pull  -->

## pull

`pull` no Git é basicamente trazer as atualizações do repositório remoto pro seu projeto local.

- Evite pull com código não salvo;

- Antes de dar pull: `git status`;

Se tiver alteração não salva:

- faça commit 
- ou use stash:
```
git stash
git pull
git stash pop
```

